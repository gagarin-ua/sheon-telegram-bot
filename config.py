import os
import logging
from dotenv import load_dotenv

# Загружаем переменные окружения из .env файла (если он существует локально)
load_dotenv()

# --- Настройки логгирования ---
# Устанавливаем базовую конфигурацию логгирования
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)

# --- Настройки подключения ---
# Получаем BOT_TOKEN из переменных окружения (важно для Render)
BOT_TOKEN = os.getenv("BOT_TOKEN")

# Получаем WEBHOOK_URL из переменных окружения (если используется webhook)
WEBHOOK_URL = os.getenv("WEBHOOK_URL")

# Проверка, что токен существует
if not BOT_TOKEN:
    logging.error("BOT_TOKEN не найден. Проверьте переменные окружения.")
    # Для продакшена лучше выбросить исключение или остановить программу
    # exit(1)

# --- Константы для текста и данных ---
WELCOME_TEXT = (
    "Ласкаво просимо у світ автентичної біжутерії з натурального каміння SHEON! \\n\\n"
    "Наш вироби створені для жінок, які цінують унікальність та витонченість. \\n\\n"
    "Це не просто аксесуари – це деталі, що підкреслять ваш стиль і особистість, "
    "залишаючи легкий акцент на вашій впевненості. \\n\\n"
    "Будьте певні: кожна прикраса існує в єдиному екземплярі і створена саме для вас."
    "\\n\\n Ми можемо відтворити настрій, який відчуєте при носінні!"
)

# Константы для данных (например, для callback_data)
# Это помогает избежать "магических строк" в коде
CALLBACK_DATA_STONES = "stones_menu"
CALLBACK_DATA_CATALOG = "catalog_and_prices"

# URL для кнопки "Каталог і ціни"
CATALOG_URL = "https://t.me/your_channel_link" 
# Замените на реальную ссылку